# into the void

a place to say it. let it go.
nothing is saved. ever.

---

a privacy-first venting app prototype. open the landing, then try the web version.

- `index.html` — landing page
- `try.html` — interactive prototype

no build step. no dependencies. just open in a browser, or deploy as a static site.
